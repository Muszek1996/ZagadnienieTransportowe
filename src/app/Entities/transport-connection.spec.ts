import { TransportConnection } from './transport-connection';

describe('TransportConnection', () => {
  it('should create an instance', () => {
    expect(new TransportConnection()).toBeTruthy();
  });
});
